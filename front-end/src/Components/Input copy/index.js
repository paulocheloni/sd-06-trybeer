import React from 'react';

import CompInput from './styles';

const Input = ({ placeholder, width, heigth }) => (
  <CompInput placeholder={ placeholder } width={ width } heigth={ heigth } />
);

export default Input;
