const register = require('./register.controllers');
const profile = require('./profile.controllers');
const adminProfile = require('./adminProfile.controllers');

module.exports = {
  register,
  profile,
  adminProfile,
};
